<Window x:Class="AppDirect.WindowsClient.UI.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AppDirect.WindowsClient.UI"
        Title="MyApps" Height="330" Width="680" 
        WindowStartupLocation="Manual" 
        Background="Transparent"
        WindowStyle="None"
        AllowsTransparency="True"
        DataContext="{DynamicResource MainViewModel}">
    <Window.Resources>
        <ContextMenu x:Key="UninstallContextMenu">
            <MenuItem Header="Uninstall" Click="UninstallAppClick" DataContext="{Binding}"/>
        </ContextMenu>
        <Style x:Key="AppButton" TargetType="Button" BasedOn="{StaticResource BorderlessButtonWithShadow}">
            <Setter Property="Height" Value="44"/>
            <Setter Property="Width" Value="160"/>
            <Setter Property="VerticalAlignment" Value="Top"/>
            <Setter Property="ContextMenu" Value="{StaticResource UninstallContextMenu}"/>
        </Style>
        <DataTemplate x:Key="EmptyDataTemplate">
            <Button Style="{DynamicResource BorderlessButton}"  Width="160" >
                <Button.Background>
                    <LinearGradientBrush EndPoint="0.504,1.5" StartPoint="0.504,0.03">
                        <GradientStop Color="Transparent" Offset="0"/>
                        <GradientStop Color="Transparent" Offset="0.3"/>
                    </LinearGradientBrush>
                </Button.Background>
                <Image Source="Images\NoApp.png"></Image>
            </Button>
        </DataTemplate>
        <DataTemplate x:Key="AppDataTemplate">
            <Button Style="{DynamicResource AppButton}" Command="{Binding LaunchApp}">
                
                <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Width="160" Height="44" Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="44"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" Width="36" Height="36" Source="{Binding ImagePath}" />
                    <TextBlock FontSize="14" Grid.Column="1" TextTrimming="CharacterEllipsis" Foreground="White" Padding="8,4,0,0" VerticalAlignment="Top" Text="{Binding Name}"></TextBlock>
                </Grid>
            </Button>
        </DataTemplate>
        <DataTemplate x:Key="AppDataWithAlertTemplate">
            <Button Style="{DynamicResource AppButton}" Command="{Binding LaunchApp}">
               
                <Grid HorizontalAlignment="Left" VerticalAlignment="Top" Width="160" Height="44" Margin="0">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="44"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <Image Grid.Column="0" VerticalAlignment="Center" HorizontalAlignment="Center" Width="36" Height="36" Source="{Binding ImagePath}" />
                    <Border Grid.Column="0" CornerRadius="15" Width="15" Height="15" Margin="29,29,0,0" Padding="0,0,0,0" Background="Red" BorderBrush="White" BorderThickness="0">
                        <TextBox FontSize="10" FontWeight="ExtraBold" IsReadOnly ="True" TextAlignment = "Center" BorderThickness ="0" Background ="Transparent" Foreground = "White" Width = "15" VerticalAlignment = "Center" Text ="{Binding AlertCount}"  />
                    </Border>
                    <TextBlock FontSize="14" Grid.Column="1" TextTrimming="CharacterEllipsis" Foreground="White" Padding="8,4,0,0" VerticalAlignment="Top" Text="{Binding Name}"></TextBlock>
                </Grid>
            </Button>
        </DataTemplate>
        <local:TemplateSelector x:Key="TemplateSelector"
              EmptyDataTemplate="{StaticResource EmptyDataTemplate}"
              AppDataTemplate="{StaticResource AppDataTemplate}"
              AppDataWithAlertTemplate="{StaticResource AppDataWithAlertTemplate}"/>
    </Window.Resources>
    <Grid Name="ContentGrid" DataContext="{Binding Source={StaticResource MainViewModel}}" Margin="0,0,0,0" >
        <Rectangle Fill="#0b1318" HorizontalAlignment="Left" Width="40" Margin="0,0,0,0"/>
        <Rectangle Fill="#181e1f" Opacity="0.92" HorizontalAlignment="Left" Width="600" Margin="40,0,0,0"/>
        <TabControl Background="Transparent" HorizontalAlignment="Left" Width="440" BorderThickness="0" TabStripPlacement="Left">
            <TabItem Name="YourAppsTab" Background="Transparent" Margin="-2,6,6,-6">
                <TabItem.Header>
                    <Image Source="Icons/icon.png" Width="28.857" />
                </TabItem.Header>
                <Grid Name="YourApps" Margin="10,30" Height="260" Width="360" VerticalAlignment="Top" HorizontalAlignment="Left">
                    <ListBox BorderThickness="0" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Name="MyAppsListBox" ItemsSource="{Binding MyApplications}" ItemTemplateSelector="{StaticResource TemplateSelector}">
                        <ListBox.ItemsPanel>
                            <ItemsPanelTemplate>
                                <WrapPanel IsItemsHost="True"/>
                            </ItemsPanelTemplate>
                        </ListBox.ItemsPanel>
                        <ListBox.Resources>
                            <Style TargetType="ListBoxItem">
                                <Setter Property="Focusable" Value="False"></Setter>
                                <Setter Property="Margin" Value="4,0,0,0"></Setter>
                            </Style>
                        </ListBox.Resources>
                    </ListBox>
                </Grid>
            </TabItem>
            <TabItem Name="SettingsTab" Background="Transparent" Margin="-2,20,6,-20">
                <TabItem.Header>
                    <Image Source="Images/cog.png" Width="24" />
                </TabItem.Header>
                <Grid Name="SettingsGridView" Margin="30,27.715,30,0" Height="260" Width="340" VerticalAlignment="Top">
                    <Rectangle Fill="#323131"  HorizontalAlignment="Left" Width="340"/>
                    <Label FontSize="14" Foreground="White" Content="Log in to your account"  HorizontalAlignment="Left" Height="33" Margin="27,10,0,0" VerticalAlignment="Top" Width="176"/>
                    <TextBlock Name="YouMustBeLoggedInMessage" TextTrimming="CharacterEllipsis" Margin="27,29,34,202" Foreground="DarkTurquoise" Text="You must be logged in to install " Visibility="Hidden"/>
                    <Label Name="LoginFailedMessage" Margin="27,165.77,124,70.23" Foreground="DarkTurquoise" Visibility="Hidden">Incorrect Username or Password</Label>
                    <Button Content="Log In" Style="{DynamicResource MainButtonStyle}" HorizontalAlignment="Left" Margin="239,200,0,0" VerticalAlignment="Top" Width="67" Height="28" Click="Login"/>
                    <TextBox Name="UsernameTextBox" Style="{DynamicResource UserNameTextBox}" VerticalAlignment="Top" HorizontalAlignment="Left" Margin="23.5,58,0,0"/>
                    <TextBox Name="PasswordTextBox"  Style="{DynamicResource PasswordTextBox}" Margin="23.5,108.5,37,76.5" TextAlignment="Justify" HorizontalAlignment="Center" />
                    <Button Content="Recover Password" HorizontalContentAlignment="Center" Click="ForgotPassword" Style ="{DynamicResource BorderlessButton}" Margin="107,200,137,36" FontSize="11"></Button>
                    <Button Content="Register" HorizontalContentAlignment="Center" Click="ForgotPassword" Style ="{DynamicResource BorderlessButton}" Margin="23.5,200,269,36" FontSize="11"></Button>
                </Grid>
            </TabItem>
        </TabControl>
        <ListBox BorderThickness="0" Opacity="80" Background="Transparent" ScrollViewer.HorizontalScrollBarVisibility="Disabled" Margin="441,59,-1,66" Name="SuggestedListBox" ItemsSource="{Binding SuggestedApplications}" HorizontalAlignment="Left" Width="200" Grid.ColumnSpan="2">
            <ListBox.Resources >
                <Style TargetType="ListBoxItem">
                    <Setter Property="Focusable" Value="False"></Setter>
                    <Setter Property="Margin" Value="2"></Setter>
                </Style>
            </ListBox.Resources>
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <Button Style="{DynamicResource InstallAppButton}" Height="24"  Margin="0,0,0,0" Click="InstallAppClick">
                        <StackPanel HorizontalAlignment="Left" Orientation="Horizontal" VerticalAlignment="Top" Width="140">
                            <Image HorizontalAlignment="Left" Width="16" Height="16" Source="{Binding ImagePath}" />
                            <TextBlock TextTrimming="CharacterEllipsis" Foreground="White" Width="124" Margin="6, 2" VerticalAlignment="Center" Text="{Binding Name}"></TextBlock>
                        </StackPanel>
                    </Button>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        <Button Style="{DynamicResource BorderlessButton}" BorderBrush="Transparent" Height="28" Content="Browse Apps" VerticalAlignment="Bottom" Margin="441,0,0,10" Click="GoToAppStore" HorizontalAlignment="Left" Width="133" Grid.ColumnSpan="2">
        </Button>
        <Label Name="SuggestedAppsLabel" Foreground="White" Content="Popular One-click Installs" HorizontalAlignment="Left" Margin="441,24.04,0,0" VerticalAlignment="Top" RenderTransformOrigin="0.53,0.539" Grid.ColumnSpan="2"/>
        <Image Source="Images\Line.png" Margin="421.5,50,0,77" HorizontalAlignment="Left" Width="19.5" Grid.ColumnSpan="2" />
    </Grid>
</Window>

